"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Menu,X,ArrowUpRight} from "lucide-react";import {useEffect,useRef,useState} from "react";import {site} from "@/data/site";
export function Header(){const path=usePathname(),[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false),menu=useRef<HTMLDivElement>(null);useEffect(()=>{const f=()=>setScrolled(scrollY>30);f();addEventListener("scroll",f,{passive:true});return()=>removeEventListener("scroll",f)},[]);useEffect(()=>{const e=(x:KeyboardEvent)=>{if(x.key==="Escape")setOpen(false)};addEventListener("keydown",e);document.body.style.overflow=open?"hidden":"";return()=>{removeEventListener("keydown",e);document.body.style.overflow=""}},[open]);return <header className={`header ${scrolled?"header--scrolled":""}`}><div className="shell header-in"><Link href="/" className="logo" aria-label="REPIKI, главная">REPIKI</Link><nav aria-label="Основная навигация">{site.nav.slice(1).map(x=><Link className={path===x.href?"active":""} key={x.href} href={x.href}>{x.label}</Link>)}</nav><Link className="talk" href="/contact">Обсудить проект <ArrowUpRight size={16}/></Link><button className="menu" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu" aria-label="Открыть меню">{open?<X/>:<Menu/>}</button></div>{open&&<div ref={menu} id="mobile-menu" className="mobile-menu" role="dialog" aria-modal="true"><div className="mobile-links">{site.nav.map((x,i)=><Link style={{animationDelay:`${i*60}ms`}} onClick={()=>setOpen(false)} key={x.href} href={x.href}>{x.label}</Link>)}</div><p>Доступен для новых проектов</p></div>}</header>}
