"use client";import{useState}from"react";import{Copy,Check}from"lucide-react";import{site}from"@/data/site";export function CopyEmailButton(){const[ok,setOk]=useState(false);return <button className="copy" onClick={async()=>{await navigator.clipboard.writeText(site.email);setOk(true);setTimeout(()=>setOk(false),1800)}}>{ok?<Check size={18}/>:<Copy size={18}/>} {ok?"Email скопирован":"Скопировать email"}</button>}
