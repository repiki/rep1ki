"use client";import{useForm}from"react-hook-form";import{z}from"zod";import{zodResolver}from"@hookform/resolvers/zod";const schema=z.object({name:z.string().min(2,"Укажите имя"),contact:z.string().min(4,"Укажите способ связи"),message:z.string().min(10,"Расскажите о задаче чуть подробнее")});type Values=z.infer<typeof schema>;export function ContactForm(){const{register,handleSubmit,formState:{errors,isSubmitted}}=useForm<Values>({resolver:zodResolver(schema)});return <form className="contact-form" onSubmit={handleSubmit(()=>undefined)} noValidate><label>Имя<input {...register("name")} placeholder="Как к вам обращаться"/>{errors.name&&<small>{errors.name.message}</small>}</label><label>Telegram или email<input {...register("contact")} placeholder="Чтобы я мог ответить"/>{errors.contact&&<small>{errors.contact.message}</small>}</label><label>О проекте<textarea {...register("message")} placeholder="Что вы хотите запустить?" rows={5}/>{errors.message&&<small>{errors.message.message}</small>}</label><button className="btn gradient" type="submit">Отправить заявку</button>{isSubmitted&&<p className="form-demo">Форма работает в демонстрационном режиме. Для быстрой связи напишите в Telegram: @qstyqq</p>}</form>}
